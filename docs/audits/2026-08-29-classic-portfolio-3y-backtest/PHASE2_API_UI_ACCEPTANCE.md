# 子阶段2：API 与研究页面验收

- 日期：2026-08-29
- 状态：通过

## 完成内容

- 组合回测 API 新增经典组合情景研究结果及独立审计产物 `22_classic_portfolio_three_year_study.json`。
- 策略回测页默认选择五个经典组合，并固定展示 2023-08-29 至 2026-08-28 的三年研究区间。
- 页面展示执行假设、逐标的数据真实性、五条优选净值曲线、5×6 调仓矩阵、最后12个月验证指标、下一决策/执行日及完整模拟交易流水。
- 原有正式交易门禁保持不变，页面只允许 `RESEARCH / OBSERVE / COMPARE / PLAN_DRAFT`。

## 验收证据

| 项目 | 结果 |
| --- | --- |
| 后端生产构建 | 通过 |
| 前端生产构建 | 通过，3732 个模块完成转换 |
| API 契约回归 | 通过，11 个策略模板可读取 |
| 真实浏览器请求 | 通过，五个策略和六种情景均写入请求体 |
| 页面结果渲染 | 通过，图表、矩阵、官方分红标签和流水均可见 |
| 浏览器控制台 | 0 个错误 |

## PRD 检视

- 具体代码、固定权重、交易时点、整数手、成本和调仓频率均在页面可见。
- 交易流水为模拟账本，不写入真实订单或真实持仓。
- 结果区同时保留数据等级、模型有效性和正式交易锁定信息，不会把回测结果伪装为直接交易建议。

## 审计意见

未发现致命或重大规格偏差。11MB 研究产物在本机真实请求下约 17.4 秒返回，属于可用但仍可后续分页优化的非阻断性能风险。
