# 90 日网格实验：前端验收与 PRD 规格检视

- 状态：通过
- 数据：真实数据库记录 `f1af049a-35b9-47f9-9dfa-a6434953389f`
- 浏览器：Orca 内嵌 Chromium，真实 Vite 代理连接真实后端

## 自动化验收

- `npm run verify:overnight-strategy-runtime`：通过。
- `npm run build`：TypeScript 与 Vite 生产构建通过；隔夜策略独立产物约41.59kB（gzip约15.99kB）。大体积 vendor chunk 为既有构建警告，不阻断本功能。
- 页面无演示回退：接口失败时不生成参数表或假热力图；并发数据库缺陷已按09号审计闭环后重验。

## 真实浏览器路径

1. 打开 `/overnight-strategy`，切换“证据与回测”→“90日放宽实验”。
2. 页面显示5,832组训练参数、2,230行信号特征、1,620组有成交、0组通过训练门禁、校正 `p=0.9525`。
3. 页面显示60日训练→唯一候选门禁→30日冻结留出；无候选时留出只显示原始基线。
4. 基线留出0笔，平均收益和3%/5%触及率显示“不可估计”，并明确“0笔不等于0%收益”。
5. 收益排序首组明确标注“不是推荐参数”“仅1笔”；没有“最佳参数”、买入或提交委托动作。
6. 参数表由服务端分页：点击下一页后页码由1变为2，首个数据排名由1变为26。
7. 热力图指标从“训练成交数”切换为“平均单笔净收益”后，ARIA说明更新为 `averageNetReturnPct`，仍为18个训练单元格。
8. 取消“只看有成交组合”后页码回到1，总数从1,620变为5,832。
9. 全过程无错误 Alert；四个交易权限持续为 `false`。

## 响应式与可访问性

- 桌面视口验证页面结论、卡片、图表和分页表完整显示。
- iPhone 12仿真为390×844 CSS像素：文档宽度与视口均为390，无页面级横向溢出；实验面板宽357；参数表宽1360并被限制在可横向滚动的表格容器中。
- 一级、二级页签均有 `tab/tabpanel` 语义；热力图有文本说明；选择器有“热力图横轴/纵轴/指标”标签；按钮与复选框可键盘访问。

## PRD 逐项检视

| 规格 | 结果 |
| --- | --- |
| 长期证据与短窗口实验隔离 | 通过：独立二级页签和 `evidenceMode` |
| 5,832组、60/30防泄漏 | 通过 |
| 只用训练指标绘制热力图 | 通过：`training only` 和固定参数标签 |
| 0笔不伪装0% | 通过 |
| 极值不包装为推荐 | 通过 |
| 服务端分页和筛选 | 通过 |
| 数据不足和交易隔离 | 通过 |
| 移动端表格可用 | 通过 |

没有新增致命或重大规格偏差。前端子阶段准予关闭。
