# 第一子阶段：核心引擎验收

日期：2026-08-30

## 已实现

- 场景曲线增加绝对总资产、人民币盈亏及逐日持仓权重快照。
- 新增 `registry_fixed` 研究服务；组合频率从策略注册表读取，当前五个经典组合均映射到季度调仓。
- 新增每周首个共同交易日起投分析、逐策略指标与横截面汇总。
- 少于 20 个交易日的年化结果为空并标记 insufficient。

## 验收证据

- 后端 TypeScript 构建通过。
- 场景引擎测试通过；绝对资产与仓位曲线点数一致。
- 每个仓位快照的现金与成分权重合计误差不超过 0.05 个百分点。
- 交易仍满足整数手、最低佣金、现金不透支和次日执行约束。

## PRD 规格检视

- 新路径没有调用 `preferredScenario`，也没有比较六种规则后改变交易规则。
- 滚动起点只保存指标汇总，未把每个窗口的完整曲线加入返回结构。
- 未修改实盘动作权限或真实持仓。

## 审计结论

未发现致命或重大规格偏差，可进入 API、持久化与页面子阶段。
