# 子阶段4：证据API与前端工作台验收

日期：2026-08-28
结论：隔夜策略API、前端构建、静态契约与真实浏览器交互通过

## 自动化与页面验收

- `npm run test:overnight-strategy-api-e2e`：通过。
  - 协议API返回300笔/24个月、64位预注册哈希和四个false交易边界。
  - 真实回测API返回 `decision=data_insufficient`、V2逐项门禁和未晋级状态。
  - 前向API可创建冻结研究运行，最新运行查询一致，且不允许创建委托。
- `npm run build`（frontend）：通过；`npm run verify:overnight-strategy-runtime`：通过。
- 使用本地Chrome实际打开 `/overnight-strategy` 并切换“证据与回测”：通过。
  - 页面显示冻结时间线、`data_insufficient` 中文解释、0.000240%正式覆盖、300笔/24月要求和逐项门禁。
  - 缺少正式成交时净收益、PF、回撤、集中度及基线/消融/成本均显示“未计算”，没有以0冒充结果。
  - 前向按钮在历史门禁未通过时不可执行；真实交易隔离四字段可见。

## PRD规格检视

| 规格 | 结果 |
|---|---|
| 四态结论不混淆 | 通过 |
| 时间线、哈希和逐项门禁 | 通过 |
| 基线、消融、成本的空证据语义 | 通过 |
| 前向运行版本冻结与前置门禁 | 通过 |
| 键盘可达的tab、表格和按钮 | 通过 |
| 不创建真实委托 | 通过 |

## 审计意见闭环

浏览器首轮检查发现正式PF/回撤/集中度沿用了旧研究引擎的0/100值；已改为正式成交为空时返回null，页面复验均显示“未计算”。

全仓后端TypeScript构建当前受未提交的 `external-brain` 类型导出不一致影响；该目录不属于本功能且为用户现存改动，未擅自修改。隔夜模块的API、真实数据库和运行时测试均独立通过。此项列为最终仓库集成阻塞，不伪报全仓构建成功。
