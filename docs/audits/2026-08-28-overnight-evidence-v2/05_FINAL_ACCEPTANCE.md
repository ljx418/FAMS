# 子阶段5：最终真实数据E2E与审计结论

日期：2026-08-28
结论：软件交付通过；策略有效性证据不足，结论固定为 `data_insufficient`，不得晋级或创建真实委托。

## 最终验收快照

最终默认全主板运行：

- Backtest ID：`970c77c7-b6a9-409e-8e96-1d7a323a2c5f`
- 范围：`full_main_board`
- 上海时间区间：2019-01-01 至 2026-08-27
- 状态：`completed`
- 决策：`data_insufficient`
- 证据等级：`smoke_only`
- 晋级状态：`insufficient`
- 阻断项：27项
- 正式5分钟目标覆盖率：0.00024014526387011503%
- 当前正式区间1分钟/5分钟可用覆盖：0%；本地768根真实5分钟线发生在2026-08-28，位于冻结区间结束日之后，因此不计入正式区间。
- 点时股本覆盖率：0%
- 冻结样本成交：0笔；冻结样本跨度配置：24个月
- 平均单笔净收益、Profit Factor、最大回撤、月度利润集中度：均为 `null`，页面显示“未计算”，未用0伪装成测算结果。
- 本次运行耗时：188ms；当前Lakehouse存储：251,983 bytes；正式目标约319,806,432根5分钟线。

交易边界：

- `formalTradingUnlocked=false`
- `autoTradeUnlocked=false`
- `canCreateOrder=false`
- `orderCreateAllowed=false`

## 自动化验收结果

| 验收项 | 命令或方法 | 结果 |
|---|---|---|
| 数据仓、幂等、真实免费源、组合成交、统计四态、前向状态机、真实回测与API E2E | `npm run test:overnight-evidence-v2` | 通过 |
| 后端TypeScript生产构建 | `npm run build` | 通过 |
| Prisma schema | `prisma validate`、`prisma db push`、`prisma generate` | 通过，数据库已同步 |
| 前端生产构建 | `npm run build` | 通过；仅有既有bundle体积警告 |
| 前端隔夜策略契约 | `npm run verify:overnight-strategy-runtime` | 通过 |
| 差异格式 | `git diff --check` | 通过 |
| Chrome真实浏览器 | 打开 `/overnight-strategy` 并进入“证据与回测” | 通过 |

真实源抽检通过：BaoStock免费源对 `600000` 的2025-08-28数据返回48根5分钟线；未发现可用于候选成交验证的1分钟证据，组合引擎按规格拒绝成交。Lakehouse本地样本包含768根、16只证券、1个交易日，正式分母为3,481只证券、1,914个交易日、319,806,432根。

浏览器复验确认：

- 页面显示“数据不足，不能判断策略”，并明确“这不等于策略无效”。
- 页面显示27个阻断项、0.000240% / 98%的正式覆盖率和0 / 300笔冻结样本成交。
- 缺失绩效显示“未计算”；简单基线、匹配基线、沪深300、八步消融和成本压力不生成虚假结果。
- “启动冻结观察”仅为影子研究入口；页面顶部持续显示四个交易权限为false。

## PRD规格检视

| PRD规格 | 结果 | 审计说明 |
|---|---|---|
| 2019–2026点时全主板数据合同 | 软件合同通过，真实数据不足 | 明示319,806,432根正式分母，未偷换为当前小样本 |
| 5分钟筛选与候选1分钟成交 | 通过 | 缺少1分钟证据时拒绝成交，日线代理无晋级权 |
| 组合、仓位、手数、现金与成本 | 通过 | 组合测试覆盖最多3只、单票/组合上限、100股、资金和2倍/3倍成本 |
| 开发/冻结/前向隔离 | 通过 | 时间线冻结；前向参数变化会关闭旧运行并重新开始 |
| 四态结论 | 通过 | `effective`、`ineffective`、`inconclusive`、`data_insufficient` 均有确定性测试 |
| 统计与多重比较 | 通过 | 固定种子分块Bootstrap、75组Reality Check等价校正 |
| 三类基线 | 通过 | 简单、匹配和沪深300均要求配对Bootstrap 95%下界大于0 |
| 八步消融 | 通过 | 仅报告增量证据，不复述“少一步必踩雷” |
| 真实交易隔离 | 通过 | API、数据库结果和页面均不允许创建委托 |
| 中文证据工作台 | 通过 | 真实浏览器检查结论、门禁、空值、时间线、成本和前向入口 |

## 审计意见闭环

1. 子阶段4首轮浏览器检查发现无正式成交时PF、回撤和集中度沿用旧研究值；已改为 `null` 并复验显示“未计算”。
2. 最终规格检视发现统计门禁仅显式校验简单与匹配基线；已增加沪深300配对分块Bootstrap门禁，缺少基准数据时失败。全套测试、后端构建和页面复验重新通过。
3. 子阶段4记录的 `external-brain` 类型冲突为并行工作区瞬时状态；最终后端全仓TypeScript构建已通过，该阻塞关闭。
4. 免费全量回填约3.2亿根5分钟线属于高资源动作，系统要求显式确认；付费正式数据采购金额未获授权，保持 `null`，没有自动采购。
5. 前向影子最早从2026-09-01开始，真实时间尚未经过。测试仅验证状态机，不以模拟时间冒充真实前向结果。

关闭审计意见后未发现新增致命或重大软件规格偏差。当前不能闭环的是外部数据授权、历史数据完整性和真实前向等待，不是代码测试失败。

## 自动化开发停止原因

交易决策级证据V2的软件工作流和已被文档支撑的开发计划均已实现并验收通过。策略证明在此停止，因为尚缺2019–2026完整点时全主板5分钟数据、候选1分钟成交片段、股票池/股本/交易状态/涨跌停价/公司行为/沪深300及第二源校验数据，并且2026-09-01起3–6个月的真实前向观察尚未完成。当前结论只能是 `data_insufficient`，不是 `ineffective`。未经人类确认未启动约3.2亿根免费数据回填，未经人类批准未购买正式数据，未创建任何真实买卖单。
