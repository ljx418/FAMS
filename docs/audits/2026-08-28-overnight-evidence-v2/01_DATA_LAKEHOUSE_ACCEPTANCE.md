# 子阶段1：列式行情仓验收与PRD检视

日期：2026-08-28
结论：软件与真实小样本链路通过；正式历史覆盖不足，正确保持 `data_insufficient`

## 自动化验收

- `npm run test:overnight-lakehouse`：通过。
  - 从真实Prisma规范表读取768根5分钟线、16只证券、1个交易日。
  - Silver写入16个Parquet分区；重复运行后仍为16个文件、768行。
  - 日期为2026-08-28 09:35–15:00；重复0行、非法OHLCV 0行。
- `npm run test:overnight-lakehouse-real-data`：通过。
  - BaoStock真实读取600000在2025-08-28的48根5分钟线。
  - Bronze原始响应、Silver分区、请求状态和Manifest完整。
  - 单一免费源质量分与OHLCV合法性分离，合法行未误报。
- `npm run build`：通过。

## 数据真实性与覆盖

- 当前点时分母使用本地规范主板身份清单3481只，而非已有分钟线的16只。
- 正式目标约319,806,432根5分钟线；当前正式覆盖率0.000240%。
- 当前Manifest状态为 `data_insufficient`，没有把单日数据包装为正式回测就绪。
- 完整免费五年下载需要显式确认字符串，并有25GB剩余空间硬门禁；默认交互任务最多50只。

## PRD规格检视

| 规格 | 结果 |
|---|---|
| 真实5分钟数据，不使用日线代理 | 通过 |
| 覆盖率使用全主板分母 | 通过 |
| 幂等、可恢复、可审计 | 通过 |
| 亿级数据不进入SQLite | 通过 |
| 覆盖不足禁止晋级 | 通过 |
| 交易能力保持关闭 | 通过 |

## 审计意见闭环

时间毫秒被误解析为纳秒的问题在首轮真实验收中发现并修复；质量分0.82被误判为坏OHLCV的问题在第二轮真实验收中发现并修复。复测无新增致命或重大偏差，允许进入组合级引擎子阶段。
