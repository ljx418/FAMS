# 子阶段 6：端到端验收与停止条件审计

## 验收标准

1. Prisma 校验/生成、后端构建、前端构建、规则/草案/回测单测全部通过。
2. 真实行情验证必须包含证券快照、至少21根日线、真实分钟线和沪深300，不允许 mock。
3. API 端到端验证 overview、readiness、scan、draft、backtest 和交易边界。
4. 浏览器端验证路由、导航、三页签、节点交互和真实数据不足状态。
5. `git diff --check` 通过，不覆盖无关用户改动。
6. 审计报告明确已完成项、真实数据缺口和自动化开发停止原因。

## 开发/验收前审计意见

- **重大风险（已闭环）**：真实数据验收不能只验证“接口有响应”；分别断言日线数量、分钟数量、基准报价、来源链接和扫描持久化。
- **重大风险（已闭环）**：当前数据不足不能作为策略有效性的证明；最终报告必须区分“软件验收通过”和“策略证据未晋级”。
- **重大风险（已闭环）**：视觉静态脚本无法发现对比度问题；已加入真实浏览器截图复核。

结论：无新增致命或重大未闭环意见，可以执行最终验收。

## 最终执行结果

- `prisma validate`：通过。
- 后端 TypeScript 构建：通过。
- 前端契约验证与 TypeScript/Vite 生产构建：通过。
- 确定性规则、资金草案、严格退出/滚动样本外测试：全部通过。
- 真实数据扫描：通过；快照2只、日线30根、分钟线241根、沪深300实时报价4609.18，扫描 `35f328ce-ae52-44f1-aeb8-901dfa868b1c` 正确为 `preview_only`。
- API 端到端：通过；扫描 `bbf5578c-cc66-4a4e-9f28-57944805fb29`，回测 `477c29eb-95e7-479d-864e-0e298d3c007f`。
- 真实回测门禁：分钟覆盖0.13%、历史股本覆盖0%、样本外0笔，因此 `promotionStatus=insufficient`；符合 PRD，未虚假晋级。
- 浏览器端到端：导航、三个页签、八步节点、候选、草案、回测均通过；发现并闭环深色页头对比度问题。
- 四个交易开关在服务、API和页面均保持 false。
- `git diff --check`：通过。

## PRD 最终检视

软件工作流、审计证据、交易隔离和真实数据不足门禁均已按 V1 规格实现。当前不能宣称“策略有效”或生成可行动默认参数，因为三年分钟与历史股本覆盖尚未达到90%，样本外也未达到100笔/12个月。此差距在产品中以数据就绪度和 `insufficient` 展示，而不是以模拟或日线代理填补。

## 自动化开发停止原因

本轮停止原因为：**文档完整支撑的隔夜强势策略 V1 软件开发与验收已完成；策略证据因真实历史数据覆盖不足而保持未晋级。** 继续补齐全市场三年5分钟数据属于长时数据建设任务，不能绕过现有覆盖率门禁，也不影响本轮软件功能验收结论。

**最终审计结论：软件验收通过；策略晋级不通过（真实证据不足）。**
