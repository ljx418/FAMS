# V2-PX D0 开发前基线固化计划

日期：2026-08-28
状态：已执行

## 目标

在不修改 V2-PX 生产代码的前提下，关闭独立文档审计登记项，冻结用户实施批准、真实数据基线、阶段审计机制和 Git 边界，为 PX1 提供可复核入口。

## 执行边界

- 允许修改 V2-PX 权威文档、状态源和阶段审计文档。
- 独立报告 `docs/V2_PX_INDEPENDENT_AUDIT_REPORT.md` 保留原始 `CONDITIONAL_PASS`，不得由修订者自改为外部 PASS。
- `backend/data/a-share-quote-list-cache.json` 是既存无关修改，禁止纳入 V2-PX 提交。
- 正向验收使用真实 SQLite、Operation、Daily Review 和 Chat 数据；故障注入只用于负例。

## 工作项

1. 交叉复核 BLK-01～03、HR-01～08 与 Draw.io 三项补强在合同、架构、计划、状态源和图中的唯一落点。
2. 运行后端构建、前端构建、PX0 语义合同与 Git whitespace 检查。
3. 记录真实数据数量与数据库哈希，创建 Git 忽略的可恢复副本。
4. 把 2026-08-28 用户指令登记为 PX1～PX6 顺序自动实施批准；保留 PX6 最终人工体验确认。
5. 为 PX1 单独生成开发计划和验收审计，致命/重大入场问题清零后才允许代码开发。

## 完成定义

- 文档登记项均有 CLOSED 落点，未篡改独立报告。
- 构建和语义基线通过。
- 数据库副本与源文件 SHA-256 一致。
- 权威状态不再声称“尚未获得开发批准”。
- PX1 入场审计为 PASS，且明确不能宣称 PX1 已实现。
