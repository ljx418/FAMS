# DRV1-12 首轮失败验收审计

日期：2026-08-25（Asia/Shanghai）

- reviewId：`1a4bbf74-f673-4b66-b2c5-0b7d6e6f5337`
- 结果：`failed`（正确执行强制门禁）
- 确定性状态：`completed`，六项持仓成功、资产错误为零
- LLM：真实尝试一次，`fallback / llm_schema_invalid`

## 打回结论

本轮不得用于最终验收。Run 与 Operation 均已失败，未沿用规则 fallback 冒充真实 LLM 成功，说明新增强制门禁生效。

## 根因定位与开发计划修订

现有失败码只说明严格 schema 未通过，缺少安全的字段路径诊断；同时供应商请求仅靠提示约束 JSON。修订如下：

1. 为 schema 失败保存 Zod 字段路径、错误类型和消息，不保存模型原文或密钥。
2. 对 OpenAI 兼容请求增加 `response_format=json_object`，同时继续执行本地严格 schema、标的和证据白名单校验。
3. 显式关闭 SDK 网络重试，保证每轮在网络层也只发起一次供应商请求。
4. 使用全新 reviewId 重跑；若供应商不支持 JSON mode 或仍结构非法，继续失败，不降低门禁。

## 再开发准入

上述修订不改变任何行情、均线、估值、网格或数量计算，不放宽证据边界；无新增致命或重大风险，允许进入第二轮实现和验收。
